name: CI — Platform A (Consumer)

on:
  pull_request:
    paths:
      - "apps/consumer/**"
      - "packages/shared/**"
      - "packages/ui/**"
      - "packages/db/**"
      - "packages/api-client/**"

concurrency:
  group: ci-platform-a-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build & Test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: pnpm/action-setup@v4
        with:
          version: 9
      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: "pnpm"
      - run: pnpm install --frozen-lockfile
      - run: pnpm run typecheck --filter=@8gent/consumer...
      - run: pnpm run lint --filter=@8gent/consumer...
      - run: pnpm run build --filter=@8gent/consumer...
      - run: pnpm run test --filter=@8gent/consumer...
